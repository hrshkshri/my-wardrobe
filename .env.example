# Database - Supabase PostgreSQL connection string
# You can find this in your Supabase project settings under Database > Connection string
# Format: postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL="postgresql://postgres:password@db.example.supabase.co:5432/postgres?schema=public"

# Server Configuration
PORT=3000
NODE_ENV=development

# Logging Level
# Options: debug, info, warn, error
LOG_LEVEL=debug

# Supabase Configuration
# Find these in your Supabase project settings under API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# JWT Configuration
# Generate 32+ character random strings using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_32_character_random_string_for_access_tokens_here
JWT_REFRESH_SECRET=your_32_character_random_string_for_refresh_tokens_here

# CORS Configuration
# Frontend origins are configured in src/utils/corsOptions.ts
# In development: allows any origin
# In production: only allows origins in the allowedOrigins array

# Redis Cache Configuration (Optional)
# Redis is used for caching and session management
# Can be disabled by setting REDIS_ENABLED=false or omitting these variables
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379

# For production/cloud Redis services, use REDIS_URL instead:
# REDIS_URL=redis://username:password@host:port/db
# Example for Redis Cloud: redis://default:password@redis-12345.c123.us-east-1-2.ec2.cloud.redislabs.com:12345
