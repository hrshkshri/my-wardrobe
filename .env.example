# Database - Neon PostgreSQL connection strings
# Get these from your Neon project console
# Both DATABASE_URL and DIRECT_URL should point to the same database
DATABASE_URL="postgresql://user:password@ep-xxxx.us-east-1.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxxx.us-east-1.neon.tech/neondb?sslmode=require"

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS & Frontend Configuration
FRONTEND_URL=http://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Cookie Configuration (for httpOnly cookies across subdomains)
# In development: leave empty
# In production: set to the root domain (e.g., .hrshkshri.me)
COOKIE_DOMAIN=

# Logging Level
# Options: debug, info, warn, error
LOG_LEVEL=debug

# JWT Configuration
# Generate 32+ character random strings using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_32_character_random_string_for_access_tokens_here
JWT_REFRESH_SECRET=your_32_character_random_string_for_refresh_tokens_here

# CORS Configuration
# Frontend origins are configured in src/utils/corsOptions.ts
# In development: allows any origin
# In production: only allows origins in the allowedOrigins array

# Redis Cache Configuration (Optional)
# Redis is used for caching and session management
# Can be disabled by setting REDIS_ENABLED=false or omitting these variables
REDIS_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379

# For production/cloud Redis services, use REDIS_URL instead:
# REDIS_URL=redis://username:password@host:port/db
# Example for Redis Cloud: redis://default:password@redis-12345.c123.us-east-1-2.ec2.cloud.redislabs.com:12345
